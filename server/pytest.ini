[tool:pytest]
# pytest configuration for the chat-box server
pythonpath = .
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --disable-warnings
markers = 
    integration: integration tests
    unit: unit tests
